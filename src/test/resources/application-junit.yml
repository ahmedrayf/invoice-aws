spring:
  application:
    name: onboarding
  data:
    mongodb:
      uri: mongodb://localhost:27017
      database: test
      auto-index-creation: true


aws:
  region: us-east-1
  access:
    key: fake-access-key
    secret-key: fake-secret-key
  s3:
    bucket-name: fakeB-bucket
  sqs:
    name: invoices
    queue:
      url: "http://localhost:5235/invoices"

processing:
  batch:
    size: 100